{
  "name": "securelens-security-engine",
  "version": "1.0.0",
  "description": "SecureLens Role 3 - Fact-based Security Analysis Engine and NRD Pipeline",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test test/*.test.js",
    "nrd:start": "node -e \"const SecurityEngine = require('./index'); const NRDProcessor = require('./nrd/nrd.processor'); new NRDProcessor(new SecurityEngine()).processBatch(['example-nrd.com']);\""
  },
  "dependencies": {
    "playwright": "^1.40.0",
    "punycode": "^2.3.1",
    "whois-json": "^2.0.4"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}
