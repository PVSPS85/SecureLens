Perform a FINAL UI/UX QUALITY PASS on the existing SecureLens prototype.

IMPORTANT:
This is ONLY a cleanup pass.

DO NOT add new features.
DO NOT create new pages.
DO NOT change the product architecture.
DO NOT redesign completed pages.
DO NOT change the SecureLens color palette.
DO NOT change the navigation structure.
DO NOT add unnecessary content.
DO NOT replace existing realistic demo data.

Preserve all functionality and page structure that already exists.

Focus only on visual consistency, spacing, alignment, usability, and obvious UI mistakes.

==================================================
1. GLOBAL VISUAL CONSISTENCY
==================================================

Make sure every page uses the same:

- Typography hierarchy
- Font sizes
- Font weights
- Border radius
- Card styling
- Border colors
- Shadows
- Button styling
- Input styling
- Status badges
- Risk colors
- Icon sizing
- Spacing system

The SecureLens interface should feel like one professional security product.

==================================================
2. SIDEBAR
==================================================

Verify the collapsible sidebar works consistently.

Expanded:
- Icons + labels visible.

Collapsed:
- Icons remain visible.
- Labels disappear.
- Main content gets the additional horizontal space.

Make sure:
- No labels overlap.
- No icons become misaligned.
- Active navigation remains obvious.
- The collapse button is easy to find.

Do not change the navigation items.

==================================================
3. PAGE WIDTH AND SPACING
==================================================

Fix obvious layout problems across the application.

Avoid:
- Huge empty white areas
- Excessive vertical spacing
- Cards that are unnecessarily tall
- Extremely narrow content areas
- Inconsistent left/right margins
- Uneven gaps between sections

Keep pages compact and information-dense like a professional security dashboard.

==================================================
4. TABLES
==================================================

Standardize all tables.

Make sure:
- Headers align with their columns.
- Rows have consistent height.
- Risk badges are aligned.
- Scores are aligned.
- Dates do not overlap.
- Action buttons remain visible.
- Long domains wrap cleanly instead of breaking the layout.

Do not make tables unnecessarily tall.

==================================================
5. RISK SYSTEM
==================================================

Use the same visual language everywhere:

LOW = green
MEDIUM = amber/yellow
HIGH = orange
CRITICAL = red

Risk badges, scores, icons and indicators should use consistent styling.

Do not introduce additional risk colors.

==================================================
6. FORMS AND BUTTONS
==================================================

Standardize:

- Search fields
- URL/domain/IP input
- Buttons
- Dropdowns
- Toggles
- Filter controls

Make sure buttons have clear hover/active/disabled states where appropriate.

Do not make buttons excessively large.

==================================================
7. INVESTIGATION EXPERIENCE
==================================================

Do a visual consistency check between:

Dashboard
Investigate
Investigation Report

They should clearly feel like parts of the same workflow.

Do NOT change the investigation data or architecture.

Make sure:
- Investigation input is clearly the primary action.
- Investigation Summary is easy to find.
- Website/Page Preview is visible.
- Investigation Timeline is readable.
- Technical evidence sections are properly aligned.
- The report does not feel visually cluttered.

==================================================
8. SCANNERS
==================================================

Make sure the specialized scanner page does not visually duplicate the main Investigate workflow.

The main investigation remains the primary URL/domain/IP analysis workflow.

Specialized scanners remain:

QR Scanner
Email Scanner
Phone Scanner

Chrome remains the ONLY supported browser extension.

==================================================
9. RESPONSIVE / DESKTOP QUALITY
==================================================

Optimize the current desktop layout for normal laptop and desktop screen sizes.

Prevent:
- Horizontal scrolling
- Content being cut off
- Overlapping cards
- Text running outside containers
- Buttons being pushed outside cards
- Broken sidebar layouts

Do not completely redesign the mobile experience.

==================================================
10. EMPTY STATES
==================================================

Where a page genuinely has no data, use a professional empty state instead of a completely blank white page.

Example:

"No investigations yet"
"Start an investigation to see results here."

However, do NOT add unnecessary empty-state cards to pages that already contain content.

==================================================
11. DEMO / PROTOTYPE LABELING
==================================================

Keep the prototype disclaimer where appropriate.

Do not make the interface claim that SecureLens is currently performing real-time security analysis if the current prototype uses illustrative/demo data.

Do not remove existing prototype disclaimers.

==================================================
12. FINAL POLISH
==================================================

Check every existing page for obvious:

- Alignment errors
- Typographical inconsistencies
- Broken spacing
- Duplicate UI
- Incorrect icons
- Missing labels
- Truncated text
- Overflow
- Inconsistent card sizes
- Inconsistent buttons
- Poor visual hierarchy

Fix ONLY obvious issues.

IMPORTANT FINAL RULE:

Do not make creative changes just for the sake of changing something.

If an existing component already looks correct, LEAVE IT ALONE.

The goal is:

"Same SecureLens product, but polished, consistent, professional and presentation-ready."

STOP after completing this cleanup pass.