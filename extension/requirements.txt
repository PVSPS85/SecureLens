# SecureLens Backend Integrator Python Dependencies
# 
# To install all dependencies, run the following command in your terminal:
#   pip install -r requirements.txt
#
# These dependencies are required for building and running the backend API integration
# that powers the SecureLens Chrome extension domain risk scanner.

fastapi>=0.100.0
uvicorn>=0.22.0
requests>=2.31.0
pydantic>=2.0.0
python-dotenv>=1.0.0
urllib3>=2.0.0
